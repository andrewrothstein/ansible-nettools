---
- name: url related packages...
  package: name={{item}} state=present
  with_items:
    - traceroute
    - curl
    - wget

- name: bind utils
  when: ansible_os_family == 'RedHat'
  package: name={{item}} state=present
    - 'bind-utils'
